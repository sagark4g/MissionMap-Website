// src/config.ts
export const API_BASE = "https://figma-mission-map-api.vercel.app";
export const USE_STUB = false; // set true to test UI without backend
// NOTE: Absolute URL is required because the site is hosted on a different origin (Figma/Make).